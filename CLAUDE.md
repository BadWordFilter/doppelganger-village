# CLAUDE.md — 도플갱어 마을 탈출 (Escape from Doppelganger Village)

Unity 6 WebGL 멀티플레이 추리/공포 게임.
**NAN 2026 (NHN Game × AI 해커톤) 사전과제 제출용 수직 슬라이스.** 완성작이 아니라 "돌아가는 코어 루프 + AI 활용 증명"이 목적. 범위를 늘리는 제안보다 마감을 지키는 결정을 우선할 것.
**주의: 구기획(택배 왔습니다 / 배달 루프 / 위조범 역할)은 전면 폐기됨. 이 문서가 유일한 기준이다.**

## 마감·제출물 — 2026-08-10 (월)

5개 전부 제출해야 하며 하나라도 누락 시 심사 제외:

1. **플레이 가능한 빌드 + 전체 소스** — 웹(브라우저 실행), GitHub Pages 링크
2. **플레이 영상 30~60초** — YouTube 링크
3. **게임 소개·설명 문서** (개요 / 플레이 방법 / 실행 방법) — PDF
4. **AI 활용 기술 문서** (AI 도구·프롬프트·활용 내역 정리) — PDF
5. **팀원 롤 기술서** (팀원별 역할·담당 영역) — PDF

## 기술 스택 (고정 — 변경 제안 금지)

- Unity **6000.3.21f1 LTS**, **Universal 3D (URP)** 템플릿
- 타깃 플랫폼: **WebGL 단일** (PC 크롬 기준, 모바일 대응 불필요)
- 멀티플레이: **Photon PUN 2** — 마스터 클라이언트 권한 방식, 룸 코드 입장
- 프로젝트 경로: `C:\Users\probo\EscapefromDoppelgangerVillage`
- Product Name: `도플갱어 마을 탈출` / C# / 신형 Input System / 주석·UI 텍스트는 한국어

## 게임 컨셉 (기획서 요약)

- 스토리: 택배 트레일러를 타고 이동하다 사고로 마을에 고립. 마을은 주민으로 위장한 도플갱어에게 점령됨. 대화로 진짜 주민을 가려내 구출하고, 트레일러 수리 부품을 모아 탈출한다.
- 승리: 진짜 주민 n마리 + 수리 부품 n개를 모으면 트레일러를 고치고 즉시 탈출 (일수 제한 없음)
- 패배: 생존 플레이어 전원이 도플갱어에게 감염되면 게임 오버
- 플레이어 상태: **HP** (0이 되면 감염 판정) / **스태미나** (Shift 달리기 소모, 정지·걷기 시 회복, 고갈 시 감속)
- 낮 페이즈 루프: 탐색 → 집 방문 → 동물과 대화 (마리당 최대 질문 3회) → 판정 선택
  - `<트레일러로 이동하세요>` = 진짜 주민으로 확정해 안전구역으로 보냄
  - `<눈을 감고 거울 비추기>` = 도플갱어로 확정해 퇴치
- **4번째 질문 시도 = 과잉 심문**: 도플갱어가 눈치채고 돌변해 공격
- 정답(진짜를 보냄): 확률적으로 구급상자/식량/수리 부품 드랍 후 안전구역으로 이동
- 오답(도플갱어를 보냄): 막을 수 없이 트레일러로 걸어가며, 해질녘 정산 때 구출 주민 수 차감
- 식별 기믹 5종 (플레이어에게 절대 비고지, 스스로 학습하게 설계): ① 생태적 모순 ② 기억·정보 오류 ③ 반향어·기계적 지연 ④ 감정 결여·기괴한 반응 ⑤ 제4의 벽 돌파
- 대화 확률: **일상 질문에는 도플갱어가 100% 정상적으로 거짓말**. 핵심 질문에서만 질문 차수에 따라 본색 노출 — 1번째 10% / 2번째 30% / 3번째 60% 확률로 이상 답변
- 조작: WASD 이동 / 마우스 시야 전환 / Shift 달리기 / E·좌클릭 상호작용 / F·우클릭 거울 등 특수 행동 / Tab 과제 확인 / ESC 메뉴
- 톤 레퍼런스: 도코데모 잇쇼, 딩컴 (귀여운 로우폴리 마을 → 기괴한 반전)

## 사전과제 빌드 범위 (이것만 구현)

- **낮 페이즈 1개만.** 해질녘은 정산 화면 1장으로 압축. 밤 페이즈 없음.
- 맵: 기본 거주 구역 축소판 — 집 5~6채 + 트레일러(안전구역)
- 동물 3종: **강아지 / 고양이 / 토끼**. 등장 개체 6~8마리, 그중 도플갱어 2~3 (마스터가 게임 시작 시 랜덤 배정)
- 대화 시스템은 **데이터 드리븐**: 기획서 대화 테이블의 강아지/고양이/토끼 분량을 JSON으로 이식. **임의 창작 금지** — 이 테이블이 이 게임 최대 자산이다.
- 대화 UI: 일상/핵심 섞인 질문 선택지 3개 제시, 마리당 3회 제한, 위 확률 규칙으로 정상/이상 답변 분기
- 판정: E = 트레일러로 보내기, F = 거울 비추기. **진짜 주민에게 거울 = 주민이 겁먹고 도주(구출 실패 처리)** ← 기획서 미정 사항의 슬라이스 기본값
- 승리 수치: 주민 3 + 부품 3 (상수로 분리해 튜닝 가능하게)
- HP 감소는 과잉 심문 공격만. 배회·추격은 스트레치.
- 1~4인 PUN co-op, 4자리 룸 코드 입장, **1인으로도 시작 가능해야 함** (심사위원이 혼자 테스트)
- 로드맵으로 미룸 (구현 금지, 소개서 로드맵 섹션용): 9종 동물 200개 테이블 전체, 밤 페이즈·야행성 구출, 일차별 맵 확장(상업/의료·산업/외곽), 개인 과제 체크리스트, 감염자 플레이, 야간 인원 결정 투표

## 데이터 규칙

- `Assets/Data/dialogue.json` 스키마: `{ id, animal, type: "일상"|"핵심", question, normalAnswer, doppelAnswer }`
- 괄호로 된 연출 지문형 답변(예: "(목이 720도 회전하며…)")도 텍스트 그대로 이식하고, UI에서 지문 스타일로 구분 표시
- 확률 테이블·승리 수치·도플갱어 수는 상수 클래스 또는 ScriptableObject로 분리

## 스트레치 골 (순서 고정 — 코어 루프 + 배포 검증 후에만)

1. **과잉 심문 후 추격**: 돌변한 도플갱어가 NavMesh 3상태 FSM(배회→감지→추격)으로 플레이어를 쫓음. 잡히면 HP 감소 + 짧은 기절. 사망/관전 시스템은 만들지 않는다. 심사위원 혼자서도 체감되는 요소라 보이스보다 우선.
2. **근접 보이스**: Photon Voice 2, 푸시투토크(V 홀드), Speaker AudioSource Spatial Blend 1.0. 착수 전 DemoVoiceMinimal 씬을 WebGL로 빌드해 마이크/출력 먼저 검증. 타임박스 2~3시간, 초과 시 접는다.
- **LLM으로 NPC/몬스터를 실시간 제어하는 방식 금지** (지연·비용·동기화). WebGL 클라이언트에 API 키를 넣지 말 것 — 공개 리포라 키가 그대로 노출된다. LLM 인게임 통합은 본선/출시 과제.

## 멀티 아키텍처 규칙

- **마스터 클라이언트가 모든 판정 권한**: 도플갱어 배정, 대화 확률 굴림(전 클라이언트 동일 결과 보장), 구출/퇴치 판정, 정산, 승패 계산
- 역할·상태는 Player/Room CustomProperties, 상호작용은 RPC, 위치 동기화는 PhotonTransformView
- 게임 상태 전파는 이벤트 기반으로 최소화. 재접속/호스트 마이그레이션은 스코프 외.

## WebGL·배포 (중요 함정)

- Publishing Settings → **Compression Format: Disabled** (GitHub Pages는 Brotli/Gzip Content-Encoding 헤더를 못 줌. 유지하려면 Decompression Fallback ON)
- 빌드 출력을 리포의 `docs/`에 넣고 GitHub Pages(main 브랜치 /docs)로 서빙
- Photon은 WebGL에서 WSS 사용 — PhotonServerSettings에서 확인만
- **첫날에 빈 씬이라도 빌드→배포까지 검증할 것.** 배포 함정은 마지막 날 밟으면 못 고친다.

## 리포 규칙

- 리포명: `doppelganger-village` (전체 소스가 제출물이다)
- Unity용 .gitignore 필수: `Library/ Temp/ Obj/ Logs/ UserSettings/ Build/` 등 제외. 단 `docs/`(WebGL 빌드 산출물)는 커밋 대상
- 커밋 메시지에 Claude 활용 내역 한 줄 포함 — 예: `feat: 대화 확률 분기 (Claude로 구현)`

## AI 활용 로그 — 매 세션 필수 (제출물 4번의 원천)

- `submission/ai-log.md`에 세션마다 append: 날짜 / 작업 내용 / 사용한 프롬프트 요지 / 결과와 수동 수정 내역
- 세션을 마칠 때 이 파일 업데이트를 빠뜨리지 말 것. 이 로그가 그대로 "AI 활용 기술 문서" PDF 본문이 된다.
- PDF 원고 등 제출 문서 작업물도 `submission/`에 둔다.

## 사람이 직접 해야 하는 일 (Claude가 대신 처리하지 않음)

- [ ] Photon 계정 생성 → 대시보드에서 앱 2개 생성 (PUN AppId + Voice AppId) → PhotonServerSettings에 입력
- [ ] 에셋스토어에서 **Photon Voice 2** (free) 임포트 — PUN 2가 포함되어 있으므로 PUN 2를 별도로 임포트하지 말 것
- [ ] GitHub 리포 생성 + Pages 활성화 (main /docs)
- [ ] Kenney / Quaternius 등 CC0 로우폴리 동물(강아지·고양이·토끼)·마을 에셋 다운로드 후 임포트
- [ ] YouTube 영상 업로드, PDF 변환, 최종 제출

## 구현 순서

1. PUN 연결 + 룸 코드 로비 + 2~4인 이동 동기화 (3인칭 카메라 마우스 시야, Shift 달리기 + 스태미나 게이지)
2. 빈 씬이라도 WebGL 빌드 → GitHub Pages 배포 검증 ← **첫날 필수**
3. 마을 미니맵 + 집/동물 배치 + 대화 UI (JSON 로드, 질문 3회 제한, 확률 분기)
4. 판정(E/F) + 아이템 드랍 + 트레일러 수리 게이지 + 과잉 심문 공격 + 정산·승패 화면
5. 톤 연출 최소 (밝은 마을 BGM → 이상 징후 시 사운드 전환, 거울 연출 컷)
6. (스트레치 1) 과잉 심문 후 추격 FSM
7. (스트레치 2) 근접 보이스
8. 영상 30~60초 촬영 (4인 + 대화 판별 + 거울 퇴치 장면 중심) → PDF 3종 → 제출
